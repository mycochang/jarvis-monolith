[project]
name = "jarvis-monolith"
version = "1.0.0"
description = "A zero-latency voice dictation monolith using faster-whisper and evdev."
requires-python = ">=3.10"
dependencies = [
    "faster-whisper",
    "sounddevice",
    "numpy",
    "scipy",
    "evdev",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
